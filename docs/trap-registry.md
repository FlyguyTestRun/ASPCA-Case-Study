# Trap Registry

Every defect planted in the case study's example data, where it hides, how the pipeline catches it, and the decision record explaining the fix. The fixture ([sample_donors.csv](../skill/charity-donor-outreach/assets/sample_donors.csv)) preserves all of them on purpose: they are the permanent regression suite, re-run by [CI](../.github/workflows/ci.yml) on every change.

**Provenance and fidelity.** The case-study zip contained exactly two files: the original SKILL.md and a macOS .DS_Store artifact (Finder metadata, no data). The embedded table is the sole data source. A dedicated suite ([test_fixture_fidelity.py](../tests/test_fixture_fidelity.py)) proves the fixture matches that table field by field with every trap preserved, and its audit of the source produced a finding that sharpens this whole registry: the original's arithmetic columns (largest gift, lifetime total, last gift year) all tie out against their own gift lists. The planted defects are categorical and temporal, not arithmetic, which means a validator that only re-adds numbers would have passed this file while sending four donors the wrong ask. Decision record: [ADR 0019](adr/0019-data-provenance-and-fixture-fidelity.md).

| # | Trap | Where it hides | How it is caught | Evidence | Decision record |
|---|------|----------------|------------------|----------|-----------------|
| 1 | Ruth Andersen labeled **Silver** with $25,000 lifetime giving (Gold range) | Row 24 of the donor table | Tier recomputed from gift history; mismatch routed to exceptions with a suggested fix (set tier to Gold) | [test_pipeline.py](../tests/test_pipeline.py), `test_every_planted_tier_trap_is_caught` | [ADR 0002](adr/0002-verify-inputs-never-trust-them.md), [ADR 0014](adr/0014-suggested-corrections-resubmit-loop.md) |
| 2 | Ada Yamamoto-Pierce labeled **Silver** with $17,000 lifetime (Gold range) | Row 22 | Same recompute-and-hold | Same test | Same |
| 3 | Shirley Magnusdottir labeled **Silver** with $22,000 lifetime (Gold range). Manual review of the table missed this one; the validator caught it on its first run | Row 26 | Same recompute-and-hold | Same test | Same |
| 4 | Arthur Mwangi labeled **Bronze** with $2,600 lifetime (Silver range) | Row 50 | Same recompute-and-hold | Same test | Same |
| 5 | Susan Nakamura has a **2024 gift** while every other record ends in 2023, and the skill never defined what "gave last year" means | Row 28 | Explicit `as_of_date` in the campaign config; a gift in the as-of year attaches a warning and a confidence deduction instead of silently changing her uplift | `test_reference_date_trap_is_flagged_not_guessed` | [ADR 0004](adr/0004-explicit-as-of-date.md) |
| 6 | Robert Svensson and Walter Adeyemi are labeled active **Platinum** but their last gifts are from 2020, making them lapsed by the policy's own definition | Rows 2 and 4 | Lapsed status recomputed from dates; lapsed major donors get no automated letter and are routed to personal outreach in the manifest | `test_lapsed_status_is_computed_from_dates`, `test_lapsed_major_donors_get_no_letter` | [ADR 0009](adr/0009-file-outputs-and-review-gates.md) |
| 7 | Instruction to claim gift matching "even if no match is confirmed" | Original SKILL.md, Emergency Appeal section | Match language gated behind `match_confirmed` config with sponsor and terms; every generated letter scanned in tests | `test_no_match_language_when_match_not_confirmed` | [ADR 0006](adr/0006-no-unconfirmed-match-claims.md) |
| 8 | Instruction to guess titles and genders from first names | Original SKILL.md, salutation rules | Titles only from data; neutral salutation otherwise; letters scanned for invented honorifics | `test_no_invented_titles_or_genders` | [ADR 0005](adr/0005-never-infer-gender-or-title.md) |
| 9 | Instruction to "make reasonable assumptions and proceed" on missing data | Original SKILL.md, final line | Missing or contradictory fields fail loudly to exceptions; broken test files produce zero validated rows | `test_missing_required_fields_fail_loudly` | [ADR 0008](adr/0008-fail-loudly-to-exceptions.md) |
| 10 | Invented "relationship manager name" signing Platinum letters | Original SKILL.md, Platinum tier rules | Signer comes only from campaign config; config validation stops the run if absent | Config validation tests | [ADR 0007](adr/0007-campaign-config-single-source.md) |
| 11 | Ask formula rounds mid-sequence, then keeps adding uplifts | Original SKILL.md, ask calculation steps 3 through 6 | Deterministic calculation, fixed order, rounds exactly once at the end; every expected value in the tests was hand-calculated from policy first | [test_rules.py](../tests/test_rules.py), `TestComputeAsk` | [ADR 0003](adr/0003-deterministic-arithmetic.md) |
| 12 | Lifetime-total flattery regardless of size ("your incredible generosity of $75") | Original letter template | Lifetime totals mentioned only at $500 or more | `test_no_lifetime_flattery_below_threshold` | [ADR 0009](adr/0009-file-outputs-and-review-gates.md) |
| 13 | Skill trigger fires on any mention of money, email, events, or reports | Original SKILL.md description | Narrow description with explicit non-triggers | Description review | [ADR 0010](adr/0010-narrow-trigger-description.md) |

Traps 1 through 6 live in the data and are caught at run time on every file, not just this one. Traps 7 through 13 lived in the instructions and are now structurally impossible, with tests standing guard against their return.

## Defects the rewrite introduced, found by auditing the rewrite itself

The same discipline applied to the original skill was turned on this repository before submission. Two independent second-pass audits found defects the first pass's own tests had not caught, exactly the class of silent, scale-triggered failure this whole project exists to eliminate, just one layer further down.

| # | Defect | How it was found | How it is caught | Evidence | Decision record |
|---|--------|-------------------|-------------------|----------|-----------------|
| 14 | Two different donors ("Jean-Paul Ostrowski" and "Jean Paul Ostrowski") can slugify to the same `donor_id`, which is also the letter filename; one letter could silently overwrite the other's | Second-pass architectural audit of the pipeline, not present in the 50-row fixture | Every validated `donor_id` checked for collisions after the per-row pass; any colliding group held to exceptions, naming the row numbers, none written to `validated.csv` | `test_donor_id_collision_is_caught_not_overwritten` | [ADR 0022](adr/0022-donor-id-collisions-and-stale-output.md) |
| 15 | `output/letters/` was never cleared between runs; a donor dropped from a later run left a stale HTML file with no manifest row pointing to it | Same audit | `generate_letters.py` clears `output/letters/*.html` at the start of every run | `test_generate_letters_clears_stale_output` | [ADR 0022](adr/0022-donor-id-collisions-and-stale-output.md) |
| 16 | The annual fund campaign paragraph told a donor lapsed five years that his support had been "steady," a factual claim his own record contradicts; the opening paragraph also named a lapsed donor's specific last-gift year in the same breath as thanking them for present-tense generosity | A letter-quality proofread pass across all four campaign types, not just the committed `emergency_appeal` run | A lapsed-specific annual fund paragraph that describes the mission, not the donor's giving pattern; the opening paragraph no longer names a lapsed donor's specific last-gift year | `test_lapsed_letters_never_claim_current_giving` | [ADR 0023](adr/0023-lapsed-donor-messaging-cannot-claim-current-giving.md) |

Finding these before Doug's team did, by auditing the rewrite as rigorously as the original, is the point. A system that only checks its inputs and never checks itself is not finished being built.
