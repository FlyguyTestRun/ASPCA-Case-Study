# Hours Log: ASPCA Case Study

Time is logged per work block. All times US Central.

| Date | Start | End | Hours | Work performed |
|------|-------|-----|-------|----------------|
| 2026-07-09 | evening | | .5 |  First read of the charity-donor-outreach skill, initial flaw inventory, assessment approach drafted. |
| 2026-07-10 | 13:00 | 15:30 | .5 | *(From Phone.)* Repo scaffold. Donor fixture transcription (50 rows, planted errors preserved). Policy and schema implemented and included reference docs. LLM ask calculation! Took this out of the LLM, and letter generation scripts with audit traces. 46-test suite, all passing; pipeline verified end to end (4 tier traps caught, including one manual review missed). 13 ADRs. Review app built and verified in browser. CI workflow. README with pipeline DAG. Assessment document drafted. |

| 2026-07-10 | 15:45 | 17:00 | .75 | Feedback-loop expansion: suggested corrections with human-approved resubmit (CLI and UI), style learning from reviewer edits with guardrails and named adoption, tutorial mode and narrated audio walkthrough in the review app, token and process economy writeup, trap registry. Test suite grown to 61, all passing. Full fix-and-resubmit loop verified in the browser: 4 exceptions to a clean 50 of 50. |

| 2026-07-11 | 10:30 | 12:15 | 1.75 | Design review of the original skill validity-checked, professionalized, and moved into docs/design-review (7 themed files plus index). Four gaps from the review implemented: letter schema validation before render, full fail/report/pass confidence rubric with webhook-ready escalation events, CSV formula-injection defenses with upload caps, per-stage run metrics. New docs: components guide, scale-architecture trigger table. ADRs 0017 and 0018 added, 0011 revised. README rewritten concise with TOC. Test suite grown to 81; pipeline, app, and resubmit loop re-verified in browser; run log clean. |

| 2026-07-11 | 12:30 | 13:00 | 0.5 | Data provenance audit. Confirmed the original zip contained only SKILL.md plus a macOS .DS_Store artifact (no data), so the embedded 50-donor table is the sole data source. Added a fixture-fidelity test suite (5 tests): parses the original markdown table and proves the fixture matches it field by field, planted errors preserved, and independently audits that the original's arithmetic columns tie out. Suite at 86. Machine learning scoped honestly into the scale-architecture trigger table (confidence recalibration, donor analytics). |

| 2026-07-11 | 13:15 | 15:30 | 1.25 | Discoveries formalized: ADR 0019 (data provenance and fixture fidelity) and the design review updated with the corrected finding (original arithmetic ties out; traps are categorical and temporal). Operational decision log built (ADR 0020): applied corrections, style adoptions, and review sign-offs now write ADR-style entries automatically, wired into apply_corrections.py, learn_style.py, and the app. Review app rebuilt as a guided 4-step flow (Upload, Findings, Review, Finalize) with a named-approver requirement and an export gate on required reviews. Walkthrough script rewritten as a candid field-notes assessment; recording instructions written for Bryan's own voice. Built a standalone, self-contained HTML deliverable (deliverable/donor-data-review.html, ADR 0021): verified-result summary, annotated pipeline diagram, and a searchable/editable/exportable table of all 50 donors, dataset built from the real pipeline output. Found and fixed a real bug during browser testing (donors filed with tier "Lapsed" were wrongly flagged as mismatches); added a Node-based test that executes the page's own JavaScript against the fixture so the bug class cannot return unnoticed. Suite at 95, all passing. |

| 2026-07-11 | 15:45 | 17:15 | 1.5 | Rewrote the walkthrough as a tight, under-two-minute script built around one teachable idea: a well designed prompt only ever improves probability, a well designed harness converts parts of that into a guarantee. Built a real guided walkthrough into the standalone HTML deliverable: six spotlighted, captioned steps over live parts of the page (verified result, each pipeline stage, the confidence gate, a real caught error, the closing thesis), keyboard and button controls, narration audio embedded as a base64 data URI so the file stays single-file, step timing allocated proportionally to word count so it self-adjusts to any narration length. Verified all six steps, spotlight positioning against both SVG and dynamically-rendered table targets, audio playback and sync, keyboard nav, and full state reset on End, live in the browser. Found and fixed a second real headless-testing bug during this work (the DOM stub in test_deliverable_logic.py had only ever passed by accident; rebuilt it as a complete, reusable stub) and added tests enforcing the two-minute budget and six-step structure by reading the real TOUR_STEPS array and pacing constant out of the built file. Suite at 99, all passing. |

| 2026-07-12 | 09:00 | 11:15 | 1.25 | Independent second-pass audit of the rewritten pipeline against the original findings, treating it as a fresh design review rather than trusting the prior session's own account. Confirmed no disagreement with the core architecture (computed tiers over stated labels, deterministic arithmetic, fail/report/pass confidence rubric, schema-then-render, human gates, CSV injection defenses). Found two real scale-triggered defects the prior passes missed: donor_id derived by slugify() can collide for two different names, which would let one donor's letter silently overwrite another's; and generate_letters.py never cleared output/letters/ between runs, so a donor dropped from a later run left a stale file with no manifest row pointing to it. Fixed both, with two new regression tests. ADR 0022 records the finding and the fix, including the decision to defer full run-versioning as a documented scale trigger. Closed a real test-coverage gap with a parametrized test over all five required campaign config fields. Ran a letter-quality proofread pass across all four campaign types, not just the committed emergency_appeal run, and found the annual fund paragraph telling a donor lapsed five years that his support had been "steady," a factual claim his own record contradicts; fixed with a lapsed-specific paragraph variant and a fix to the opening paragraph, recorded in ADR 0023. Suite grew 99 to 107, all passing; full pipeline re-verified end to end. |
| 2026-07-12 | 11:30 | 14:00 | 2.5 | Pre-push readiness pass. Found and restored a real integrity issue: original/charity-donor-outreach/SKILL.md, the file meant to be preserved exactly as received, had been hand-annotated with design notes, undermining the verbatim-preservation claim the fixture-fidelity tests depend on; restored from the first commit, verified clean. Reviewed and verified all prior uncommitted work line by line before building on it. Closed the remaining gaps against a full production-readiness checklist: added references/donor.schema.json as a structural validation layer distinct from and prior to business-rule checks (ADR 0024); extracted the bounded-personalization prompt out of SKILL.md into its own versioned asset, prompts/personalization_prompt.md (ADR 0025); added an explicit RULES_VERSION constant stamped into every validation report, run-metrics entry, and decision-log entry, and structured JSON (.jsonl) alongside every CSV at each stage boundary (ADR 0026). Rewrote SKILL.md as an explicit pipeline-stage table naming every deterministic step and the one optional, bounded, off-by-default model step, with the reasoning for keeping the model out of the default path linked to ADR 0016 rather than silently changed. Wrote docs/requirements-checklist.md, mapping every named production-readiness control (schema validation, required field validation, input sanitization, failure handling, audit logging, structured JSON between stages, separation of prompt/data/business logic, elimination of hallucination-prone calculations, removal of unethical instructions, regression testing, versioned business rules) directly to its implementation and test. Updated the trap registry with the two rewrite-introduced defects and their fixes, updated the design review and README to link the new checklist, corrected test and ADR counts throughout. Suite grew 107 to 111, all passing; full pipeline and standalone deliverable rebuilt and re-verified against the fixture. |

| 2026-07-12 | 14:15 | 17:00 | 2 | Letter quality and UI finalization pass. Found and fixed a real robustness gap: config JSON loaders failed on a UTF-8 byte-order mark, which Notepad and PowerShell both write by default on Windows, exactly where a non-technical user editing the campaign config would hit it; fixed all three read sites, added a regression test. Found and fixed a real letter-quality defect: an empty P.S. block left a stray whitespace line before every letter's closing tag; cleaned up with a general blank-line collapse in the renderer. Built an on-demand run archive feature (donor_rules.archive_run, list_archived_runs; ADR 0027): a labeled, timestamped snapshot of a completed run's letters and manifest, since generate_letters.py clears output/letters/ every run by design; wired into the review app's Finalize step with a past-runs browser, 6 new dedicated tests. Rebuilt the review app's Upload step with real onboarding content: how to use the sample dataset (with its provenance explained) versus uploading a real file, and a script-by-script explainer of the pipeline mirroring SKILL.md's stage table, for reuse as an internal training artifact. Added live per-stage status labels during a run (st.status) naming the exact script executing, for the same teaching purpose. Rewrote ASSESSMENT.md tighter and more directly responsive to the case study's two-part task, then rebuilt ASSESSMENT.docx from it with a dedicated build script (assessment/build_docx.js) instead of a generic converter, verified by inspecting the document XML directly since no LibreOffice/pandoc was available in this environment. Created one real demonstration archive of the pre-fix output before regenerating, so the archive feature is provable, not just described. Verified extensively in the live browser (new Upload-step content, live pipeline status, sidebar and checkbox mechanics); the Finalize-stage archive button's final click was not completed live due to a synthetic-automation limitation with Streamlit's dropdown component under headless testing, not a defect in the feature, which rests on 6 passing dedicated tests and code that mirrors an already-tested function. Suite grew 111 to 118, all passing. |

| 2026-07-12 | 17:15 | 17:45 | 0.5 | Wrap-up verification. Set aside further Streamlit UI testing and instead ran a clean-room agent dry run: every command in SKILL.md executed verbatim, from a scratch directory outside the repository, with the donor file placed where an uploaded file actually would be, no reliance on prior knowledge of the build. Every documented step (validate, apply corrections, restart, calculate, generate) produced exactly the promised output files and counts; the correction loop took the run from 4 held records to 50 of 50; 48 letters generated with zero model calls, confirming the optional personalization step correctly stays off by default. Recorded in ADR 0028. Corrected stale test and ADR counts (107/111 to 118, 26 to 28 ADRs) across README, scale-architecture.md, and the design review. Suite holds at 118, all passing. |

| 2026-07-12 | 18:00 | 18:35 | 0.6 | Built docs/run-walkthrough.md: the fixture run through every real stage, captured live from an actual scratch run (not written from memory), each stop showing the exact command, the exact terminal output, and a real file excerpt, paired with a plain-language explanation and an engineering note so any technical level can follow. Includes the four planted tier corrections flowing all the way through to a changed ask amount (Ada Yamamoto-Pierce's ask recalculates at the Gold rate once her tier is fixed, not the Silver rate the mislabeled file would have used), a full worked ask-calculation trace for two donors, the structured letter object shown before it becomes HTML, and the review manifest explaining exactly which donors get mandatory review and why. Linked from the README, SKILL.md, and components.md. Caught and fixed three more stale test-count references (86, 107, 111 all corrected to 118) missed in the prior pass's sweep. Suite holds at 118, all passing. |

| 2026-07-12 | 18:45 | 19:30 | 0.75 | Full third-party name and company scrub across the repository (Doug, PDS, Inflexis, JLL/JLLT removed from `deliverable/README.md`, `docs/adr/0021`, `docs/trap-registry.md`, `assessment/ASSESSMENT.md`, `assessment/build_docx.js`, `HOURS.md`, `deliverable/donor-data-review.template.html`), own authorship kept throughout by design. Verified with a repo-wide grep sweep, including a base64-noise-filtered pass over the built HTML deliverable, and a 191-link relative-link integrity check. Followed by a professionalism and consistency pass: fixed a real typo ("corrected inding" to "corrected finding"), fixed a stale ADR count in two files (27 and 20 to the true 28), reconciled the 44-vs-48 letter count that read as a contradiction between the run walkthrough and the committed evidence, corrected `HOURS.md`'s own total against the sum of its logged blocks (11.5 to 13.6), and trimmed two verbatim-duplicated passages between the README and the design review / deliverable docs into single-source links. Rebuilt `ASSESSMENT.docx`. Suite holds at 118, all passing; all relative links resolve. |

| 2026-07-12 | 19:45 | 21:15 | 1.5 | Extended the standalone deliverable so it cleans data, not just displays it (ADR 0029). Added a dependency-free CSV parser and an upload control that accepts the case study's own unedited donor file, or any file in the same shape, and validates it with the same client-side logic already pinned against the Python pipeline. Added a Review column and a new "letter conditions" function mirroring donor_rules.confidence_score, confidence_band, and review_level exactly (blocked pending correction, routed to personal outreach, mandatory or recommended review, clear for an automated letter), something the page never surfaced before even though the pipeline enforces it. Added a one-click "Apply suggested corrections" action mirroring apply_corrections.py, and browser-side persistence (localStorage) so a cleaned dataset survives a reload and is offered back explicitly on the next visit, never auto-applied. Fixed the export to write the exact column shape and order validate_input.py reads, using the current corrected tier rather than a silently recomputed one, so the file can go straight back through the real pipeline before any letter is generated. Extended tests/test_deliverable_logic.py with a Node harness that runs the whole upload-clean-persist flow against the real unedited fixture headlessly, asserting it finds exactly what Python finds (4 mismatches, 2 lapsed majors, 5 Platinum donors under mandatory review) and that a save-then-restore round trip reproduces the corrected state; the same test writes tests/reports/deliverable_clean_metrics.json as a committed, regenerated metrics record. Verified live in the browser: upload, apply corrections, save, reload, and restore all matched the headless test's numbers exactly, with no console errors. Suite grew 118 to 122, all passing; all relative links resolve; corrected stale test and ADR counts (118 to 122, 28 to 29) across README, ASSESSMENT.md, and components.md. |

| 2026-07-12 | 21:30 | 22:15 | 0.75 | Two more requests against the deliverable, both checked with a clarifying question first rather than guessed at, since one touched a core reproducibility decision (ADR 0004): the as_of_date stays pinned at 2024-06-30 by design, so ADR 0004 now has an explicit section explaining why the demo does not silently track today's date, and the deliverable's own footer says so directly instead of reading as neglect. Last Gift Yr now displays as an honest end-of-year date (12/31/2020) since the source data only ever recorded a year, never a day; the underlying value used for edits, validation, and export stayed a plain year so the real pipeline's contract never changed. Extended build_dataset.py to run generate_letters.py as part of its own scratch build (never touching the committed output/) so the embedded dataset always carries the real generated letter for every donor the pipeline actually produced one for; each row's detail expansion now renders that letter in a sandboxed iframe next to the ask trace, or the specific reason there isn't one (held pending correction, routed to personal outreach), taken from the pipeline's own manifest text, never invented. Recorded in ADR 0030. Verified live in the browser: Earl Fontaine and James Whitfield show their real letters exactly as generated; Walter Adeyemi correctly shows no letter with the routing reason. Suite holds at 122, all passing; corrected ADR count (29 to 30) across README, ASSESSMENT.md, and components.md; rebuilt ASSESSMENT.docx and the deliverable. |

| 2026-07-12 | 22:20 | 22:50 | 0.5 | Fixed a real conflation of two different date concepts: generate_letters.py built the date printed on every letter from the campaign config's as_of_date, the same fixed 2024-06-30 reference point ADR 0004 requires for tier and lapsed-status math, so every letter carried a frozen 2024 date no matter when the pipeline actually ran. Gave letter_date its own value, defaulting to the real day the letter is generated with an optional --letter-date override for a reproducible test run, and left as_of_date untouched everywhere it drives business logic. Verified directly: a fresh run now prints "July 12, 2026" on every letter, and the override still produces a fixed date on demand. Regenerated the committed output/ evidence and the deliverable; diffed every changed letter to confirm only the date line moved, nothing structural. Added two permanent regression tests (test_letter_date_is_not_the_as_of_date, test_letter_date_override_is_respected) and recorded the finding and fix in ADR 0031 and the trap registry. Suite grew 122 to 124, all passing; corrected test and ADR counts (122 to 124, 30 to 31) across README, ASSESSMENT.md, requirements-checklist.md, scale-architecture.md, the design review, and components.md; rebuilt ASSESSMENT.docx. |

**Total: ~17.1hrs.**
