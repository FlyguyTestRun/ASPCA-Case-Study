# Hours Log: ASPCA Case Study

Time is logged per work block. All times US Central.

| Date | Start | End | Hours | Work performed |
|------|-------|-----|-------|----------------|
| 2026-07-09 | evening | | 1.5 | Received case study from Doug Maschoff. First read of the charity-donor-outreach skill, initial flaw inventory, assessment approach drafted. |
| 2026-07-10 | 13:00 | 15:30 | 2.5 | Repo scaffold. Donor fixture transcription (50 rows, planted errors preserved). Policy and schema reference docs. Validation, ask calculation, and letter generation scripts with audit traces. 46-test suite, all passing; pipeline verified end to end (4 tier traps caught, including one manual review missed). 13 ADRs. Review app built and verified in browser. CI workflow. README with pipeline DAG. Assessment document drafted. |

| 2026-07-10 | 15:45 | 17:00 | 1.25 | Feedback-loop expansion: suggested corrections with human-approved resubmit (CLI and UI), style learning from reviewer edits with guardrails and named adoption, tutorial mode and narrated audio walkthrough in the review app, token and process economy writeup, trap registry. Test suite grown to 61, all passing. Full fix-and-resubmit loop verified in the browser: 4 exceptions to a clean 50 of 50. |

| 2026-07-11 | 10:30 | 12:15 | 1.75 | Design review of the original skill validity-checked, professionalized, and moved into docs/design-review (7 themed files plus index). Four gaps from the review implemented: letter schema validation before render, full fail/report/pass confidence rubric with webhook-ready escalation events, CSV formula-injection defenses with upload caps, per-stage run metrics. New docs: components guide, scale-architecture trigger table. ADRs 0017 and 0018 added, 0011 revised. README rewritten concise with TOC. Test suite grown to 81; pipeline, app, and resubmit loop re-verified in browser; run log clean. |

| 2026-07-11 | 12:30 | 13:00 | 0.5 | Data provenance audit. Confirmed the original zip contained only SKILL.md plus a macOS .DS_Store artifact (no data), so the embedded 50-donor table is the sole data source. Added a fixture-fidelity test suite (5 tests): parses the original markdown table and proves the fixture matches it field by field, planted errors preserved, and independently audits that the original's arithmetic columns tie out. Suite at 86. Machine learning scoped honestly into the scale-architecture trigger table (confidence recalibration, donor analytics). |

| 2026-07-11 | 13:15 | 15:30 | 2.25 | Discoveries formalized: ADR 0019 (data provenance and fixture fidelity) and the design review updated with the corrected finding (original arithmetic ties out; traps are categorical and temporal). Operational decision log built (ADR 0020): applied corrections, style adoptions, and review sign-offs now write ADR-style entries automatically, wired into apply_corrections.py, learn_style.py, and the app. Review app rebuilt as a guided 4-step flow (Upload, Findings, Review, Finalize) with a named-approver requirement and an export gate on required reviews. Walkthrough script rewritten as a candid field-notes assessment; recording instructions written for Bryan's own voice. Built a standalone, self-contained HTML deliverable (deliverable/donor-data-review.html, ADR 0021): verified-result summary, annotated pipeline diagram, and a searchable/editable/exportable table of all 50 donors, dataset built from the real pipeline output. Found and fixed a real bug during browser testing (donors filed with tier "Lapsed" were wrongly flagged as mismatches); added a Node-based test that executes the page's own JavaScript against the fixture so the bug class cannot return unnoticed. Suite at 95, all passing. |

| 2026-07-11 | 15:45 | 17:15 | 1.5 | Rewrote the walkthrough as a tight, under-two-minute script built around one teachable idea: a well designed prompt only ever improves probability, a well designed harness converts parts of that into a guarantee. Built a real guided walkthrough into the standalone HTML deliverable: six spotlighted, captioned steps over live parts of the page (verified result, each pipeline stage, the confidence gate, a real caught error, the closing thesis), keyboard and button controls, narration audio embedded as a base64 data URI so the file stays single-file, step timing allocated proportionally to word count so it self-adjusts to any narration length. Verified all six steps, spotlight positioning against both SVG and dynamically-rendered table targets, audio playback and sync, keyboard nav, and full state reset on End, live in the browser. Found and fixed a second real headless-testing bug during this work (the DOM stub in test_deliverable_logic.py had only ever passed by accident; rebuilt it as a complete, reusable stub) and added tests enforcing the two-minute budget and six-step structure by reading the real TOUR_STEPS array and pacing constant out of the built file. Suite at 99, all passing. |

| 2026-07-12 | 09:00 | 11:15 | 2.25 | Independent second-pass audit of the rewritten pipeline against the original findings, treating it as a fresh design review rather than trusting the prior session's own account. Confirmed no disagreement with the core architecture (computed tiers over stated labels, deterministic arithmetic, fail/report/pass confidence rubric, schema-then-render, human gates, CSV injection defenses). Found two real scale-triggered defects the prior passes missed: donor_id derived by slugify() can collide for two different names, which would let one donor's letter silently overwrite another's; and generate_letters.py never cleared output/letters/ between runs, so a donor dropped from a later run left a stale file with no manifest row pointing to it. Fixed both, with two new regression tests. ADR 0022 records the finding and the fix, including the decision to defer full run-versioning as a documented scale trigger. Closed a real test-coverage gap with a parametrized test over all five required campaign config fields. Ran a letter-quality proofread pass across all four campaign types, not just the committed emergency_appeal run, and found the annual fund paragraph telling a donor lapsed five years that his support had been "steady," a factual claim his own record contradicts; fixed with a lapsed-specific paragraph variant and a fix to the opening paragraph, recorded in ADR 0023. Suite grew 99 to 107, all passing; full pipeline re-verified end to end. |
| 2026-07-12 | 11:30 | 14:00 | 2.5 | Pre-push readiness pass. Found and restored a real integrity issue: original/charity-donor-outreach/SKILL.md, the file meant to be preserved exactly as received, had been hand-annotated with design notes, undermining the verbatim-preservation claim the fixture-fidelity tests depend on; restored from the first commit, verified clean. Reviewed and verified all prior uncommitted work line by line before building on it. Closed the remaining gaps against a full production-readiness checklist: added references/donor.schema.json as a structural validation layer distinct from and prior to business-rule checks (ADR 0024); extracted the bounded-personalization prompt out of SKILL.md into its own versioned asset, prompts/personalization_prompt.md (ADR 0025); added an explicit RULES_VERSION constant stamped into every validation report, run-metrics entry, and decision-log entry, and structured JSON (.jsonl) alongside every CSV at each stage boundary (ADR 0026). Rewrote SKILL.md as an explicit pipeline-stage table naming every deterministic step and the one optional, bounded, off-by-default model step, with the reasoning for keeping the model out of the default path linked to ADR 0016 rather than silently changed. Wrote docs/requirements-checklist.md, mapping every named production-readiness control (schema validation, required field validation, input sanitization, failure handling, audit logging, structured JSON between stages, separation of prompt/data/business logic, elimination of hallucination-prone calculations, removal of unethical instructions, regression testing, versioned business rules) directly to its implementation and test. Updated the trap registry with the two rewrite-introduced defects and their fixes, updated the design review and README to link the new checklist, corrected test and ADR counts throughout. Suite grew 107 to 111, all passing; full pipeline and standalone deliverable rebuilt and re-verified against the fixture. |

| 2026-07-12 | 14:15 | 17:00 | 2.75 | Letter quality and UI finalization pass. Found and fixed a real robustness gap: config JSON loaders failed on a UTF-8 byte-order mark, which Notepad and PowerShell both write by default on Windows, exactly where a non-technical user editing the campaign config would hit it; fixed all three read sites, added a regression test. Found and fixed a real letter-quality defect: an empty P.S. block left a stray whitespace line before every letter's closing tag; cleaned up with a general blank-line collapse in the renderer. Built an on-demand run archive feature (donor_rules.archive_run, list_archived_runs; ADR 0027): a labeled, timestamped snapshot of a completed run's letters and manifest, since generate_letters.py clears output/letters/ every run by design; wired into the review app's Finalize step with a past-runs browser, 6 new dedicated tests. Rebuilt the review app's Upload step with real onboarding content: how to use the sample dataset (with its provenance explained) versus uploading a real file, and a script-by-script explainer of the pipeline mirroring SKILL.md's stage table, for reuse as a training artifact with Inflexis staff. Added live per-stage status labels during a run (st.status) naming the exact script executing, for the same teaching purpose. Rewrote ASSESSMENT.md tighter and more directly responsive to the case study's two-part task, then rebuilt ASSESSMENT.docx from it with a dedicated build script (assessment/build_docx.js) instead of a generic converter, verified by inspecting the document XML directly since no LibreOffice/pandoc was available in this environment. Created one real demonstration archive of the pre-fix output before regenerating, so the archive feature is provable, not just described. Verified extensively in the live browser (new Upload-step content, live pipeline status, sidebar and checkbox mechanics); the Finalize-stage archive button's final click was not completed live due to a synthetic-automation limitation with Streamlit's dropdown component under headless testing, not a defect in the feature, which rests on 6 passing dedicated tests and code that mirrors an already-tested function. Suite grew 111 to 118, all passing. |

**Total: 18.75 through the ninth pass, ready for push.**
